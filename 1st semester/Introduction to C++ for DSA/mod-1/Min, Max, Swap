#include<bits/stdc++.h>
using namespace std;

int main()
{
    int a, b;
    cin >> a >> b;

    int x = min(a, b);
    int y = max(a, b);
    swap(a, b);

    cout << "Min: " << x << "\n";
    cout << "Max: " << y << "\n";
    cout << "After swap: " << a << " " << b << "\n";

    return 0;
}
